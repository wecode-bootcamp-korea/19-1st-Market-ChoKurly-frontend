import React from 'react';

function Slider() {
  let sliderArr = [1, 2, 3, 4, 5];
  return (
    <div className="slider">
     {
       sliderArr.map((item,index)=> {
         return(

         )
       })
     }
    </div>
  );
}
